// You can combine strings using the + sign
var greeting = 'Hello'
var name = 'Andrew'

console.log(greeting + ' there, ' + name + '! How are you today?');

// You can now use backticks - much easier!
console.log(`${greeting} there, ${name}! How're you today?`);