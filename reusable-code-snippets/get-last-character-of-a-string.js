// Name variable
const fullName = 'John Smith';

// Determine the last character dynamically - in case the string is somehow modified
let lastCharacterOfString = fullName.charAt(fullName.length - 1);
